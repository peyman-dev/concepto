import React from "react";

const CompanyCategory = ({ value }: { value: string }) => {
  return (
      <p className="text-lg font-light text-[#737993]">{value}</p>
  );
};

export default CompanyCategory;
